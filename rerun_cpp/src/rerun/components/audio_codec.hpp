// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/components/audio_codec.fbs".

#pragma once

#include "../result.hpp"

#include <cstdint>
#include <memory>

namespace arrow {
    /// \private
    template <typename T>
    class NumericBuilder;

    class Array;
    class DataType;
    class UInt32Type;
    using UInt32Builder = NumericBuilder<UInt32Type>;
} // namespace arrow

namespace rerun::components {
    /// **Component**: The codec used to encode audio stored in `components::AudioChunk`.
    ///
    /// Enum values are the big-endian packing of the `WebCodecs` audio codec string
    /// (same convention used by `components::VideoCodec`).
    ///
    /// Support of these codecs by the Rerun Viewer is platform dependent.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    enum class AudioCodec : uint32_t {

        /// Free Lossless Audio Codec (FLAC).
        ///
        /// Each `components::AudioChunk` is expected to contain one or more complete FLAC frames.
        ///
        /// Enum value is the fourcc for `'flac'` (the `WebCodecs` string for FLAC) in big endian.
        Flac = 0x666C6163,

        /// Opus — RFC 6716.
        ///
        /// Streams are expected to contain one or more raw Opus packets per
        /// `components::AudioChunk`. Any sample rate supported by the decoder
        /// (8/12/16/24/48 kHz) is valid. Libopus handles internal resampling to
        /// 48 kHz but the logged `components::AudioSampleRate` is treated as
        /// authoritative for downstream analysis.
        ///
        /// Enum value is the fourcc for `'opus'` (the `WebCodecs` string for Opus) in big endian.
        Opus = 0x6F707573,
    };
} // namespace rerun::components

namespace rerun {
    template <typename T>
    struct Loggable;

    /// \private
    template <>
    struct Loggable<components::AudioCodec> {
        static constexpr std::string_view ComponentType = "rerun.components.AudioCodec";

        /// Returns the arrow data type this type corresponds to.
        static const std::shared_ptr<arrow::DataType>& arrow_datatype();

        /// Serializes an array of `rerun::components::AudioCodec` into an arrow array.
        static Result<std::shared_ptr<arrow::Array>> to_arrow(
            const components::AudioCodec* instances, size_t num_instances
        );

        /// Fills an arrow array builder with an array of this type.
        static rerun::Error fill_arrow_array_builder(
            arrow::UInt32Builder* builder, const components::AudioCodec* elements,
            size_t num_elements
        );
    };
} // namespace rerun
