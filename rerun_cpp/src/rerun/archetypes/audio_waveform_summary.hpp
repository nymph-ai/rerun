// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_waveform_summary.fbs".

#pragma once

#include "../collection.hpp"
#include "../component_batch.hpp"
#include "../component_column.hpp"
#include "../components/audio_duration_samples.hpp"
#include "../components/audio_source_id.hpp"
#include "../components/audio_timestamp.hpp"
#include "../components/entity_path.hpp"
#include "../components/scalar.hpp"
#include "../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::archetypes {
    /// **Archetype**: Multi-resolution waveform summary buckets for an audio source.
    ///
    /// Each row stores one batch of summary buckets. The `bucket_start`,
    /// `bucket_duration_samples`, `min_amplitude`, and `max_amplitude` arrays are
    /// parallel arrays and should have the same length. Optional arrays such as
    /// `rms_amplitude` and `activity` are also parallel when present.
    ///
    /// These summaries are durable derived data: they can be logged, cached,
    /// queried, exported, and rendered without decoding source audio on every pan
    /// or zoom.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct AudioWaveformSummary {
        /// Media-time start of each summary bucket, in nanoseconds.
        std::optional<ComponentBatch> bucket_start;

        /// Duration of each summary bucket, in samples per channel.
        std::optional<ComponentBatch> bucket_duration_samples;

        /// Minimum normalized amplitude in each bucket.
        std::optional<ComponentBatch> min_amplitude;

        /// Maximum normalized amplitude in each bucket.
        std::optional<ComponentBatch> max_amplitude;

        /// Optional RMS or loudness-like summary for each bucket.
        std::optional<ComponentBatch> rms_amplitude;

        /// Optional activity/silence score for each bucket. Convention: 0 is silent, 1 is active.
        std::optional<ComponentBatch> activity;

        /// Stable identity of the audio source this summary was derived from.
        std::optional<ComponentBatch> source_id;

        /// Entity carrying the source `archetypes::AudioStream` or `archetypes::AssetAudio`.
        std::optional<ComponentBatch> audio_reference;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] = "rerun.archetypes.AudioWaveformSummary";

        /// `ComponentDescriptor` for the `bucket_start` field.
        static constexpr auto Descriptor_bucket_start = ComponentDescriptor(
            ArchetypeName, "AudioWaveformSummary:bucket_start",
            Loggable<rerun::components::AudioTimestamp>::ComponentType
        );
        /// `ComponentDescriptor` for the `bucket_duration_samples` field.
        static constexpr auto Descriptor_bucket_duration_samples = ComponentDescriptor(
            ArchetypeName, "AudioWaveformSummary:bucket_duration_samples",
            Loggable<rerun::components::AudioDurationSamples>::ComponentType
        );
        /// `ComponentDescriptor` for the `min_amplitude` field.
        static constexpr auto Descriptor_min_amplitude = ComponentDescriptor(
            ArchetypeName, "AudioWaveformSummary:min_amplitude",
            Loggable<rerun::components::Scalar>::ComponentType
        );
        /// `ComponentDescriptor` for the `max_amplitude` field.
        static constexpr auto Descriptor_max_amplitude = ComponentDescriptor(
            ArchetypeName, "AudioWaveformSummary:max_amplitude",
            Loggable<rerun::components::Scalar>::ComponentType
        );
        /// `ComponentDescriptor` for the `rms_amplitude` field.
        static constexpr auto Descriptor_rms_amplitude = ComponentDescriptor(
            ArchetypeName, "AudioWaveformSummary:rms_amplitude",
            Loggable<rerun::components::Scalar>::ComponentType
        );
        /// `ComponentDescriptor` for the `activity` field.
        static constexpr auto Descriptor_activity = ComponentDescriptor(
            ArchetypeName, "AudioWaveformSummary:activity",
            Loggable<rerun::components::Scalar>::ComponentType
        );
        /// `ComponentDescriptor` for the `source_id` field.
        static constexpr auto Descriptor_source_id = ComponentDescriptor(
            ArchetypeName, "AudioWaveformSummary:source_id",
            Loggable<rerun::components::AudioSourceId>::ComponentType
        );
        /// `ComponentDescriptor` for the `audio_reference` field.
        static constexpr auto Descriptor_audio_reference = ComponentDescriptor(
            ArchetypeName, "AudioWaveformSummary:audio_reference",
            Loggable<rerun::components::EntityPath>::ComponentType
        );

      public:
        AudioWaveformSummary() = default;
        AudioWaveformSummary(AudioWaveformSummary&& other) = default;
        AudioWaveformSummary(const AudioWaveformSummary& other) = default;
        AudioWaveformSummary& operator=(const AudioWaveformSummary& other) = default;
        AudioWaveformSummary& operator=(AudioWaveformSummary&& other) = default;

        explicit AudioWaveformSummary(
            Collection<rerun::components::AudioTimestamp> _bucket_start,
            Collection<rerun::components::AudioDurationSamples> _bucket_duration_samples,
            Collection<rerun::components::Scalar> _min_amplitude,
            Collection<rerun::components::Scalar> _max_amplitude
        )
            : bucket_start(
                  ComponentBatch::from_loggable(std::move(_bucket_start), Descriptor_bucket_start)
                      .value_or_throw()
              ),
              bucket_duration_samples(ComponentBatch::from_loggable(
                                          std::move(_bucket_duration_samples),
                                          Descriptor_bucket_duration_samples
              )
                                          .value_or_throw()),
              min_amplitude(
                  ComponentBatch::from_loggable(std::move(_min_amplitude), Descriptor_min_amplitude)
                      .value_or_throw()
              ),
              max_amplitude(
                  ComponentBatch::from_loggable(std::move(_max_amplitude), Descriptor_max_amplitude)
                      .value_or_throw()
              ) {}

        /// Update only some specific fields of a `AudioWaveformSummary`.
        static AudioWaveformSummary update_fields() {
            return AudioWaveformSummary();
        }

        /// Clear all the fields of a `AudioWaveformSummary`.
        static AudioWaveformSummary clear_fields();

        /// Media-time start of each summary bucket, in nanoseconds.
        AudioWaveformSummary with_bucket_start(
            const Collection<rerun::components::AudioTimestamp>& _bucket_start
        ) && {
            bucket_start = ComponentBatch::from_loggable(_bucket_start, Descriptor_bucket_start)
                               .value_or_throw();
            return std::move(*this);
        }

        /// Duration of each summary bucket, in samples per channel.
        AudioWaveformSummary with_bucket_duration_samples(
            const Collection<rerun::components::AudioDurationSamples>& _bucket_duration_samples
        ) && {
            bucket_duration_samples = ComponentBatch::from_loggable(
                                          _bucket_duration_samples,
                                          Descriptor_bucket_duration_samples
            )
                                          .value_or_throw();
            return std::move(*this);
        }

        /// Minimum normalized amplitude in each bucket.
        AudioWaveformSummary with_min_amplitude(
            const Collection<rerun::components::Scalar>& _min_amplitude
        ) && {
            min_amplitude = ComponentBatch::from_loggable(_min_amplitude, Descriptor_min_amplitude)
                                .value_or_throw();
            return std::move(*this);
        }

        /// Maximum normalized amplitude in each bucket.
        AudioWaveformSummary with_max_amplitude(
            const Collection<rerun::components::Scalar>& _max_amplitude
        ) && {
            max_amplitude = ComponentBatch::from_loggable(_max_amplitude, Descriptor_max_amplitude)
                                .value_or_throw();
            return std::move(*this);
        }

        /// Optional RMS or loudness-like summary for each bucket.
        AudioWaveformSummary with_rms_amplitude(
            const Collection<rerun::components::Scalar>& _rms_amplitude
        ) && {
            rms_amplitude = ComponentBatch::from_loggable(_rms_amplitude, Descriptor_rms_amplitude)
                                .value_or_throw();
            return std::move(*this);
        }

        /// Optional activity/silence score for each bucket. Convention: 0 is silent, 1 is active.
        AudioWaveformSummary with_activity(const Collection<rerun::components::Scalar>& _activity
        ) && {
            activity =
                ComponentBatch::from_loggable(_activity, Descriptor_activity).value_or_throw();
            return std::move(*this);
        }

        /// Stable identity of the audio source this summary was derived from.
        AudioWaveformSummary with_source_id(const rerun::components::AudioSourceId& _source_id) && {
            source_id =
                ComponentBatch::from_loggable(_source_id, Descriptor_source_id).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `source_id` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_source_id` should
        /// be used when logging a single row's worth of data.
        AudioWaveformSummary with_many_source_id(
            const Collection<rerun::components::AudioSourceId>& _source_id
        ) && {
            source_id =
                ComponentBatch::from_loggable(_source_id, Descriptor_source_id).value_or_throw();
            return std::move(*this);
        }

        /// Entity carrying the source `archetypes::AudioStream` or `archetypes::AssetAudio`.
        AudioWaveformSummary with_audio_reference(
            const rerun::components::EntityPath& _audio_reference
        ) && {
            audio_reference =
                ComponentBatch::from_loggable(_audio_reference, Descriptor_audio_reference)
                    .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `audio_reference` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_audio_reference` should
        /// be used when logging a single row's worth of data.
        AudioWaveformSummary with_many_audio_reference(
            const Collection<rerun::components::EntityPath>& _audio_reference
        ) && {
            audio_reference =
                ComponentBatch::from_loggable(_audio_reference, Descriptor_audio_reference)
                    .value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<archetypes::AudioWaveformSummary> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(
            const archetypes::AudioWaveformSummary& archetype
        );
    };
} // namespace rerun
