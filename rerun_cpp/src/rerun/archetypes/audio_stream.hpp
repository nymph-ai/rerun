// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_stream.fbs".

#pragma once

#include "../collection.hpp"
#include "../component_batch.hpp"
#include "../component_column.hpp"
#include "../components/audio_channel_count.hpp"
#include "../components/audio_channel_layout.hpp"
#include "../components/audio_chunk.hpp"
#include "../components/audio_codec.hpp"
#include "../components/audio_discontinuity.hpp"
#include "../components/audio_duration_samples.hpp"
#include "../components/audio_priming_delay.hpp"
#include "../components/audio_sample_rate.hpp"
#include "../components/audio_seekable.hpp"
#include "../components/audio_sequence_number.hpp"
#include "../components/audio_stream_id.hpp"
#include "../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::archetypes {
    /// **Archetype**: A time-evolving stream of encoded audio chunks.
    ///
    /// `archetypes::AudioStream` is the streaming counterpart of `archetypes::AssetAudio`:
    /// each logged row is a self-contained, decodable segment and the row's
    /// timeline timestamp is treated as the presentation timestamp (PTS) of the
    /// first PCM sample in the segment.
    ///
    /// Static components (`codec`, `sample_rate`, `channel_count`, `channel_layout`,
    /// `priming_delay`) are expected to be constant over the lifetime of the
    /// stream and are typically logged once with `static=True`. The per-row
    /// components (`chunk`, `duration_samples`, `stream_id`, `sequence_number`,
    /// `discontinuity`, `seekable`) are logged with each segment.
    ///
    /// Segments may arrive out of order in the log but the viewer resumes
    /// playback from the nearest preceding seekable boundary on any seek.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct AudioStream {
        /// The codec used to encode chunks.
        std::optional<ComponentBatch> codec;

        /// Sample rate reported by / requested of the decoder, in Hz.
        std::optional<ComponentBatch> sample_rate;

        /// Number of audio channels.
        std::optional<ComponentBatch> channel_count;

        /// One decodable segment of encoded audio.
        ///
        /// The current row timestamp is used as PTS for the first sample. There is
        /// no separate DTS — Opus has no decode-order reordering; codecs that need
        /// it may be added later with an additional component.
        std::optional<ComponentBatch> chunk;

        /// Number of PCM samples per channel produced by decoding this chunk.
        ///
        /// Logged per row so the viewer never has to decode the payload to know
        /// the span length. Enables fast gap detection and scrubbing.
        std::optional<ComponentBatch> duration_samples;

        /// Speaker arrangement for the channels.
        std::optional<ComponentBatch> channel_layout;

        /// Encoder pre-roll, in samples. Discarded by the decoder at stream start.
        std::optional<ComponentBatch> priming_delay;

        /// Stream continuity marker.
        ///
        /// A change in value signals a recorder restart and is treated as an
        /// implicit discontinuity.
        std::optional<ComponentBatch> stream_id;

        /// Monotonic per-stream sequence number. Gaps surface as a diagnostic.
        std::optional<ComponentBatch> sequence_number;

        /// If true, the decoder is reset before this chunk is processed.
        std::optional<ComponentBatch> discontinuity;

        /// If true, this chunk is an independent seek target.
        ///
        /// Defaults to `true` for Opus; codecs with inter-frame dependencies may
        /// set it false on non-key segments.
        std::optional<ComponentBatch> seekable;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] = "rerun.archetypes.AudioStream";

        /// `ComponentDescriptor` for the `codec` field.
        static constexpr auto Descriptor_codec = ComponentDescriptor(
            ArchetypeName, "AudioStream:codec",
            Loggable<rerun::components::AudioCodec>::ComponentType
        );
        /// `ComponentDescriptor` for the `sample_rate` field.
        static constexpr auto Descriptor_sample_rate = ComponentDescriptor(
            ArchetypeName, "AudioStream:sample_rate",
            Loggable<rerun::components::AudioSampleRate>::ComponentType
        );
        /// `ComponentDescriptor` for the `channel_count` field.
        static constexpr auto Descriptor_channel_count = ComponentDescriptor(
            ArchetypeName, "AudioStream:channel_count",
            Loggable<rerun::components::AudioChannelCount>::ComponentType
        );
        /// `ComponentDescriptor` for the `chunk` field.
        static constexpr auto Descriptor_chunk = ComponentDescriptor(
            ArchetypeName, "AudioStream:chunk",
            Loggable<rerun::components::AudioChunk>::ComponentType
        );
        /// `ComponentDescriptor` for the `duration_samples` field.
        static constexpr auto Descriptor_duration_samples = ComponentDescriptor(
            ArchetypeName, "AudioStream:duration_samples",
            Loggable<rerun::components::AudioDurationSamples>::ComponentType
        );
        /// `ComponentDescriptor` for the `channel_layout` field.
        static constexpr auto Descriptor_channel_layout = ComponentDescriptor(
            ArchetypeName, "AudioStream:channel_layout",
            Loggable<rerun::components::AudioChannelLayout>::ComponentType
        );
        /// `ComponentDescriptor` for the `priming_delay` field.
        static constexpr auto Descriptor_priming_delay = ComponentDescriptor(
            ArchetypeName, "AudioStream:priming_delay",
            Loggable<rerun::components::AudioPrimingDelay>::ComponentType
        );
        /// `ComponentDescriptor` for the `stream_id` field.
        static constexpr auto Descriptor_stream_id = ComponentDescriptor(
            ArchetypeName, "AudioStream:stream_id",
            Loggable<rerun::components::AudioStreamId>::ComponentType
        );
        /// `ComponentDescriptor` for the `sequence_number` field.
        static constexpr auto Descriptor_sequence_number = ComponentDescriptor(
            ArchetypeName, "AudioStream:sequence_number",
            Loggable<rerun::components::AudioSequenceNumber>::ComponentType
        );
        /// `ComponentDescriptor` for the `discontinuity` field.
        static constexpr auto Descriptor_discontinuity = ComponentDescriptor(
            ArchetypeName, "AudioStream:discontinuity",
            Loggable<rerun::components::AudioDiscontinuity>::ComponentType
        );
        /// `ComponentDescriptor` for the `seekable` field.
        static constexpr auto Descriptor_seekable = ComponentDescriptor(
            ArchetypeName, "AudioStream:seekable",
            Loggable<rerun::components::AudioSeekable>::ComponentType
        );

      public:
        AudioStream() = default;
        AudioStream(AudioStream&& other) = default;
        AudioStream(const AudioStream& other) = default;
        AudioStream& operator=(const AudioStream& other) = default;
        AudioStream& operator=(AudioStream&& other) = default;

        explicit AudioStream(
            rerun::components::AudioCodec _codec, rerun::components::AudioSampleRate _sample_rate,
            rerun::components::AudioChannelCount _channel_count
        )
            : codec(ComponentBatch::from_loggable(std::move(_codec), Descriptor_codec)
                        .value_or_throw()),
              sample_rate(
                  ComponentBatch::from_loggable(std::move(_sample_rate), Descriptor_sample_rate)
                      .value_or_throw()
              ),
              channel_count(
                  ComponentBatch::from_loggable(std::move(_channel_count), Descriptor_channel_count)
                      .value_or_throw()
              ) {}

        /// Update only some specific fields of a `AudioStream`.
        static AudioStream update_fields() {
            return AudioStream();
        }

        /// Clear all the fields of a `AudioStream`.
        static AudioStream clear_fields();

        /// The codec used to encode chunks.
        AudioStream with_codec(const rerun::components::AudioCodec& _codec) && {
            codec = ComponentBatch::from_loggable(_codec, Descriptor_codec).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `codec` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_codec` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_codec(const Collection<rerun::components::AudioCodec>& _codec) && {
            codec = ComponentBatch::from_loggable(_codec, Descriptor_codec).value_or_throw();
            return std::move(*this);
        }

        /// Sample rate reported by / requested of the decoder, in Hz.
        AudioStream with_sample_rate(const rerun::components::AudioSampleRate& _sample_rate) && {
            sample_rate = ComponentBatch::from_loggable(_sample_rate, Descriptor_sample_rate)
                              .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `sample_rate` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_sample_rate` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_sample_rate(
            const Collection<rerun::components::AudioSampleRate>& _sample_rate
        ) && {
            sample_rate = ComponentBatch::from_loggable(_sample_rate, Descriptor_sample_rate)
                              .value_or_throw();
            return std::move(*this);
        }

        /// Number of audio channels.
        AudioStream with_channel_count(const rerun::components::AudioChannelCount& _channel_count
        ) && {
            channel_count = ComponentBatch::from_loggable(_channel_count, Descriptor_channel_count)
                                .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `channel_count` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_channel_count` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_channel_count(
            const Collection<rerun::components::AudioChannelCount>& _channel_count
        ) && {
            channel_count = ComponentBatch::from_loggable(_channel_count, Descriptor_channel_count)
                                .value_or_throw();
            return std::move(*this);
        }

        /// One decodable segment of encoded audio.
        ///
        /// The current row timestamp is used as PTS for the first sample. There is
        /// no separate DTS — Opus has no decode-order reordering; codecs that need
        /// it may be added later with an additional component.
        AudioStream with_chunk(const rerun::components::AudioChunk& _chunk) && {
            chunk = ComponentBatch::from_loggable(_chunk, Descriptor_chunk).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `chunk` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_chunk` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_chunk(const Collection<rerun::components::AudioChunk>& _chunk) && {
            chunk = ComponentBatch::from_loggable(_chunk, Descriptor_chunk).value_or_throw();
            return std::move(*this);
        }

        /// Number of PCM samples per channel produced by decoding this chunk.
        ///
        /// Logged per row so the viewer never has to decode the payload to know
        /// the span length. Enables fast gap detection and scrubbing.
        AudioStream with_duration_samples(
            const rerun::components::AudioDurationSamples& _duration_samples
        ) && {
            duration_samples =
                ComponentBatch::from_loggable(_duration_samples, Descriptor_duration_samples)
                    .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `duration_samples` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_duration_samples` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_duration_samples(
            const Collection<rerun::components::AudioDurationSamples>& _duration_samples
        ) && {
            duration_samples =
                ComponentBatch::from_loggable(_duration_samples, Descriptor_duration_samples)
                    .value_or_throw();
            return std::move(*this);
        }

        /// Speaker arrangement for the channels.
        AudioStream with_channel_layout(const rerun::components::AudioChannelLayout& _channel_layout
        ) && {
            channel_layout =
                ComponentBatch::from_loggable(_channel_layout, Descriptor_channel_layout)
                    .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `channel_layout` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_channel_layout` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_channel_layout(
            const Collection<rerun::components::AudioChannelLayout>& _channel_layout
        ) && {
            channel_layout =
                ComponentBatch::from_loggable(_channel_layout, Descriptor_channel_layout)
                    .value_or_throw();
            return std::move(*this);
        }

        /// Encoder pre-roll, in samples. Discarded by the decoder at stream start.
        AudioStream with_priming_delay(const rerun::components::AudioPrimingDelay& _priming_delay
        ) && {
            priming_delay = ComponentBatch::from_loggable(_priming_delay, Descriptor_priming_delay)
                                .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `priming_delay` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_priming_delay` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_priming_delay(
            const Collection<rerun::components::AudioPrimingDelay>& _priming_delay
        ) && {
            priming_delay = ComponentBatch::from_loggable(_priming_delay, Descriptor_priming_delay)
                                .value_or_throw();
            return std::move(*this);
        }

        /// Stream continuity marker.
        ///
        /// A change in value signals a recorder restart and is treated as an
        /// implicit discontinuity.
        AudioStream with_stream_id(const rerun::components::AudioStreamId& _stream_id) && {
            stream_id =
                ComponentBatch::from_loggable(_stream_id, Descriptor_stream_id).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `stream_id` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_stream_id` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_stream_id(
            const Collection<rerun::components::AudioStreamId>& _stream_id
        ) && {
            stream_id =
                ComponentBatch::from_loggable(_stream_id, Descriptor_stream_id).value_or_throw();
            return std::move(*this);
        }

        /// Monotonic per-stream sequence number. Gaps surface as a diagnostic.
        AudioStream with_sequence_number(
            const rerun::components::AudioSequenceNumber& _sequence_number
        ) && {
            sequence_number =
                ComponentBatch::from_loggable(_sequence_number, Descriptor_sequence_number)
                    .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `sequence_number` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_sequence_number` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_sequence_number(
            const Collection<rerun::components::AudioSequenceNumber>& _sequence_number
        ) && {
            sequence_number =
                ComponentBatch::from_loggable(_sequence_number, Descriptor_sequence_number)
                    .value_or_throw();
            return std::move(*this);
        }

        /// If true, the decoder is reset before this chunk is processed.
        AudioStream with_discontinuity(const rerun::components::AudioDiscontinuity& _discontinuity
        ) && {
            discontinuity = ComponentBatch::from_loggable(_discontinuity, Descriptor_discontinuity)
                                .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `discontinuity` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_discontinuity` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_discontinuity(
            const Collection<rerun::components::AudioDiscontinuity>& _discontinuity
        ) && {
            discontinuity = ComponentBatch::from_loggable(_discontinuity, Descriptor_discontinuity)
                                .value_or_throw();
            return std::move(*this);
        }

        /// If true, this chunk is an independent seek target.
        ///
        /// Defaults to `true` for Opus; codecs with inter-frame dependencies may
        /// set it false on non-key segments.
        AudioStream with_seekable(const rerun::components::AudioSeekable& _seekable) && {
            seekable =
                ComponentBatch::from_loggable(_seekable, Descriptor_seekable).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `seekable` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_seekable` should
        /// be used when logging a single row's worth of data.
        AudioStream with_many_seekable(const Collection<rerun::components::AudioSeekable>& _seekable
        ) && {
            seekable =
                ComponentBatch::from_loggable(_seekable, Descriptor_seekable).value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<archetypes::AudioStream> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(
            const archetypes::AudioStream& archetype
        );
    };
} // namespace rerun
