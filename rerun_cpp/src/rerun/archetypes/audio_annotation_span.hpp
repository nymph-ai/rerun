// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_annotation_span.fbs".

#pragma once

#include "../collection.hpp"
#include "../component_batch.hpp"
#include "../component_column.hpp"
#include "../components/audio_annotation_kind.hpp"
#include "../components/audio_source_id.hpp"
#include "../components/audio_timestamp.hpp"
#include "../components/color.hpp"
#include "../components/entity_path.hpp"
#include "../components/key_value_pairs.hpp"
#include "../components/scalar.hpp"
#include "../components/text.hpp"
#include "../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::archetypes {
    /// **Archetype**: Audio-aligned annotation spans.
    ///
    /// Use for ASR words, utterances, speaker turns, VAD regions, TTS emissions,
    /// interruption windows, and other span-like evidence.
    ///
    /// Each row stores one or more spans using parallel arrays. Spans are logged as
    /// queryable data, not private UI state, so downstream tooling can overlap-query
    /// and export them alongside audio summaries and source metadata.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct AudioAnnotationSpan {
        /// Media-time start of each span, in nanoseconds.
        std::optional<ComponentBatch> start_time;

        /// Media-time end of each span, in nanoseconds.
        std::optional<ComponentBatch> end_time;

        /// Semantic class of each span.
        std::optional<ComponentBatch> kind;

        /// Human-readable text for each span.
        std::optional<ComponentBatch> labels;

        /// Confidence score per span. Convention: 0 is lowest confidence, 1 is highest.
        std::optional<ComponentBatch> confidence;

        /// Speaker identity or diarization label per span.
        std::optional<ComponentBatch> speaker;

        /// Stable identity of the audio source these spans refer to.
        std::optional<ComponentBatch> source_id;

        /// Entity carrying the source `archetypes::AudioStream` or `archetypes::AssetAudio`.
        std::optional<ComponentBatch> audio_reference;

        /// Optional related or parent entities, e.g. words belonging to an utterance.
        std::optional<ComponentBatch> related;

        /// Additional span metadata shared by this row.
        std::optional<ComponentBatch> metadata;

        /// Optional display color per span.
        std::optional<ComponentBatch> colors;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] = "rerun.archetypes.AudioAnnotationSpan";

        /// `ComponentDescriptor` for the `start_time` field.
        static constexpr auto Descriptor_start_time = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:start_time",
            Loggable<rerun::components::AudioTimestamp>::ComponentType
        );
        /// `ComponentDescriptor` for the `end_time` field.
        static constexpr auto Descriptor_end_time = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:end_time",
            Loggable<rerun::components::AudioTimestamp>::ComponentType
        );
        /// `ComponentDescriptor` for the `kind` field.
        static constexpr auto Descriptor_kind = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:kind",
            Loggable<rerun::components::AudioAnnotationKind>::ComponentType
        );
        /// `ComponentDescriptor` for the `labels` field.
        static constexpr auto Descriptor_labels = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:labels",
            Loggable<rerun::components::Text>::ComponentType
        );
        /// `ComponentDescriptor` for the `confidence` field.
        static constexpr auto Descriptor_confidence = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:confidence",
            Loggable<rerun::components::Scalar>::ComponentType
        );
        /// `ComponentDescriptor` for the `speaker` field.
        static constexpr auto Descriptor_speaker = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:speaker",
            Loggable<rerun::components::Text>::ComponentType
        );
        /// `ComponentDescriptor` for the `source_id` field.
        static constexpr auto Descriptor_source_id = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:source_id",
            Loggable<rerun::components::AudioSourceId>::ComponentType
        );
        /// `ComponentDescriptor` for the `audio_reference` field.
        static constexpr auto Descriptor_audio_reference = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:audio_reference",
            Loggable<rerun::components::EntityPath>::ComponentType
        );
        /// `ComponentDescriptor` for the `related` field.
        static constexpr auto Descriptor_related = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:related",
            Loggable<rerun::components::EntityPath>::ComponentType
        );
        /// `ComponentDescriptor` for the `metadata` field.
        static constexpr auto Descriptor_metadata = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:metadata",
            Loggable<rerun::components::KeyValuePairs>::ComponentType
        );
        /// `ComponentDescriptor` for the `colors` field.
        static constexpr auto Descriptor_colors = ComponentDescriptor(
            ArchetypeName, "AudioAnnotationSpan:colors",
            Loggable<rerun::components::Color>::ComponentType
        );

      public:
        AudioAnnotationSpan() = default;
        AudioAnnotationSpan(AudioAnnotationSpan&& other) = default;
        AudioAnnotationSpan(const AudioAnnotationSpan& other) = default;
        AudioAnnotationSpan& operator=(const AudioAnnotationSpan& other) = default;
        AudioAnnotationSpan& operator=(AudioAnnotationSpan&& other) = default;

        explicit AudioAnnotationSpan(
            Collection<rerun::components::AudioTimestamp> _start_time,
            Collection<rerun::components::AudioTimestamp> _end_time
        )
            : start_time(
                  ComponentBatch::from_loggable(std::move(_start_time), Descriptor_start_time)
                      .value_or_throw()
              ),
              end_time(ComponentBatch::from_loggable(std::move(_end_time), Descriptor_end_time)
                           .value_or_throw()) {}

        /// Update only some specific fields of a `AudioAnnotationSpan`.
        static AudioAnnotationSpan update_fields() {
            return AudioAnnotationSpan();
        }

        /// Clear all the fields of a `AudioAnnotationSpan`.
        static AudioAnnotationSpan clear_fields();

        /// Media-time start of each span, in nanoseconds.
        AudioAnnotationSpan with_start_time(
            const Collection<rerun::components::AudioTimestamp>& _start_time
        ) && {
            start_time =
                ComponentBatch::from_loggable(_start_time, Descriptor_start_time).value_or_throw();
            return std::move(*this);
        }

        /// Media-time end of each span, in nanoseconds.
        AudioAnnotationSpan with_end_time(
            const Collection<rerun::components::AudioTimestamp>& _end_time
        ) && {
            end_time =
                ComponentBatch::from_loggable(_end_time, Descriptor_end_time).value_or_throw();
            return std::move(*this);
        }

        /// Semantic class of each span.
        AudioAnnotationSpan with_kind(
            const Collection<rerun::components::AudioAnnotationKind>& _kind
        ) && {
            kind = ComponentBatch::from_loggable(_kind, Descriptor_kind).value_or_throw();
            return std::move(*this);
        }

        /// Human-readable text for each span.
        AudioAnnotationSpan with_labels(const Collection<rerun::components::Text>& _labels) && {
            labels = ComponentBatch::from_loggable(_labels, Descriptor_labels).value_or_throw();
            return std::move(*this);
        }

        /// Confidence score per span. Convention: 0 is lowest confidence, 1 is highest.
        AudioAnnotationSpan with_confidence(const Collection<rerun::components::Scalar>& _confidence
        ) && {
            confidence =
                ComponentBatch::from_loggable(_confidence, Descriptor_confidence).value_or_throw();
            return std::move(*this);
        }

        /// Speaker identity or diarization label per span.
        AudioAnnotationSpan with_speaker(const Collection<rerun::components::Text>& _speaker) && {
            speaker = ComponentBatch::from_loggable(_speaker, Descriptor_speaker).value_or_throw();
            return std::move(*this);
        }

        /// Stable identity of the audio source these spans refer to.
        AudioAnnotationSpan with_source_id(const rerun::components::AudioSourceId& _source_id) && {
            source_id =
                ComponentBatch::from_loggable(_source_id, Descriptor_source_id).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `source_id` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_source_id` should
        /// be used when logging a single row's worth of data.
        AudioAnnotationSpan with_many_source_id(
            const Collection<rerun::components::AudioSourceId>& _source_id
        ) && {
            source_id =
                ComponentBatch::from_loggable(_source_id, Descriptor_source_id).value_or_throw();
            return std::move(*this);
        }

        /// Entity carrying the source `archetypes::AudioStream` or `archetypes::AssetAudio`.
        AudioAnnotationSpan with_audio_reference(
            const rerun::components::EntityPath& _audio_reference
        ) && {
            audio_reference =
                ComponentBatch::from_loggable(_audio_reference, Descriptor_audio_reference)
                    .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `audio_reference` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_audio_reference` should
        /// be used when logging a single row's worth of data.
        AudioAnnotationSpan with_many_audio_reference(
            const Collection<rerun::components::EntityPath>& _audio_reference
        ) && {
            audio_reference =
                ComponentBatch::from_loggable(_audio_reference, Descriptor_audio_reference)
                    .value_or_throw();
            return std::move(*this);
        }

        /// Optional related or parent entities, e.g. words belonging to an utterance.
        AudioAnnotationSpan with_related(const Collection<rerun::components::EntityPath>& _related
        ) && {
            related = ComponentBatch::from_loggable(_related, Descriptor_related).value_or_throw();
            return std::move(*this);
        }

        /// Additional span metadata shared by this row.
        AudioAnnotationSpan with_metadata(const rerun::components::KeyValuePairs& _metadata) && {
            metadata =
                ComponentBatch::from_loggable(_metadata, Descriptor_metadata).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `metadata` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_metadata` should
        /// be used when logging a single row's worth of data.
        AudioAnnotationSpan with_many_metadata(
            const Collection<rerun::components::KeyValuePairs>& _metadata
        ) && {
            metadata =
                ComponentBatch::from_loggable(_metadata, Descriptor_metadata).value_or_throw();
            return std::move(*this);
        }

        /// Optional display color per span.
        AudioAnnotationSpan with_colors(const Collection<rerun::components::Color>& _colors) && {
            colors = ComponentBatch::from_loggable(_colors, Descriptor_colors).value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<archetypes::AudioAnnotationSpan> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(
            const archetypes::AudioAnnotationSpan& archetype
        );
    };
} // namespace rerun
