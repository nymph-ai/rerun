// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/components/audio_annotation_kind.fbs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]
#![allow(non_camel_case_types)]

use ::re_types_core::SerializationResult;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentBatch as _, SerializedComponentBatch};
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Component**: Semantic class of an audio-aligned time span.
///
/// These variants cover the common observability lanes for speech and agent
/// replay. Use [`components::KeyValuePairs`][crate::components::KeyValuePairs] on the archetype for domain-specific
/// labels that do not need to become schema.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
#[derive(Clone, Copy, Debug, Hash, PartialEq, Eq)]
#[repr(u8)]
pub enum AudioAnnotationKind {
    /// A finalized or partial ASR utterance.
    AsrUtterance = 1,

    /// A word-level alignment span.
    Word = 2,

    /// A token-level alignment span.
    Token = 3,

    /// A diarization speaker turn.
    SpeakerTurn = 4,

    /// Voice activity detection region.
    Vad = 5,

    /// Endpointing or turn-boundary decision window.
    Endpointing = 6,

    /// Synthesized speech emitted by a TTS/model output path.
    Tts = 7,

    /// Overlap, interruption, or barge-in region.
    Interruption = 8,

    /// Region marked for error analysis or review.
    ErrorRegion = 9,
}

impl ::re_types_core::Component for AudioAnnotationKind {
    #[inline]
    fn name() -> ComponentType {
        "rerun.components.AudioAnnotationKind".into()
    }
}

::re_types_core::macros::impl_into_cow!(AudioAnnotationKind);

impl ::re_types_core::Loggable for AudioAnnotationKind {
    #[inline]
    fn arrow_datatype() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::UInt8
    }

    fn to_arrow_opt<'a>(
        data: impl IntoIterator<Item = Option<impl Into<::std::borrow::Cow<'a, Self>>>>,
    ) -> SerializationResult<arrow::array::ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{Loggable as _, ResultExt as _, arrow_helpers::as_array_ref};
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let (somes, data0): (Vec<_>, Vec<_>) = data
                .into_iter()
                .map(|datum| {
                    let datum: Option<::std::borrow::Cow<'a, Self>> = datum.map(Into::into);
                    let datum = datum.map(|datum| *datum as u8);
                    (datum.is_some(), datum)
                })
                .unzip();
            let data0_validity: Option<arrow::buffer::NullBuffer> = {
                let any_nones = somes.iter().any(|some| !*some);
                any_nones.then(|| somes.into())
            };
            as_array_ref(PrimitiveArray::<UInt8Type>::new(
                ScalarBuffer::from(
                    data0
                        .into_iter()
                        .map(|v| v.unwrap_or_default())
                        .collect::<Vec<_>>(),
                ),
                data0_validity,
            ))
        })
    }

    fn from_arrow_opt(
        arrow_data: &dyn arrow::array::Array,
    ) -> DeserializationResult<Vec<Option<Self>>>
    where
        Self: Sized,
    {
        use ::re_types_core::{Loggable as _, ResultExt as _, arrow_zip_validity::ZipValidity};
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok(arrow_data
            .as_any()
            .downcast_ref::<UInt8Array>()
            .ok_or_else(|| {
                let expected = Self::arrow_datatype();
                let actual = arrow_data.data_type().clone();
                DeserializationError::datatype_mismatch(expected, actual)
            })
            .with_context("rerun.components.AudioAnnotationKind#enum")?
            .into_iter()
            .map(|typ| match typ {
                Some(val) => <Self as ::re_types_core::reflection::Enum>::try_from_integer(val)
                    .map(Some)
                    .ok_or_else(|| {
                        DeserializationError::missing_union_arm(
                            Self::arrow_datatype(),
                            "<invalid>",
                            val as _,
                        )
                    }),
                None => Ok(None),
            })
            .collect::<DeserializationResult<Vec<Option<_>>>>()
            .with_context("rerun.components.AudioAnnotationKind")?)
    }
}

impl std::fmt::Display for AudioAnnotationKind {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::AsrUtterance => write!(f, "AsrUtterance"),
            Self::Word => write!(f, "Word"),
            Self::Token => write!(f, "Token"),
            Self::SpeakerTurn => write!(f, "SpeakerTurn"),
            Self::Vad => write!(f, "Vad"),
            Self::Endpointing => write!(f, "Endpointing"),
            Self::Tts => write!(f, "Tts"),
            Self::Interruption => write!(f, "Interruption"),
            Self::ErrorRegion => write!(f, "ErrorRegion"),
        }
    }
}

impl ::re_types_core::reflection::Enum for AudioAnnotationKind {
    type Repr = u8;

    #[inline]
    fn variants() -> &'static [Self] {
        &[
            Self::AsrUtterance,
            Self::Word,
            Self::Token,
            Self::SpeakerTurn,
            Self::Vad,
            Self::Endpointing,
            Self::Tts,
            Self::Interruption,
            Self::ErrorRegion,
        ]
    }

    #[inline]
    fn docstring_md(self) -> &'static str {
        match self {
            Self::AsrUtterance => "A finalized or partial ASR utterance.",
            Self::Word => "A word-level alignment span.",
            Self::Token => "A token-level alignment span.",
            Self::SpeakerTurn => "A diarization speaker turn.",
            Self::Vad => "Voice activity detection region.",
            Self::Endpointing => "Endpointing or turn-boundary decision window.",
            Self::Tts => "Synthesized speech emitted by a TTS/model output path.",
            Self::Interruption => "Overlap, interruption, or barge-in region.",
            Self::ErrorRegion => "Region marked for error analysis or review.",
        }
    }

    #[inline]
    fn try_from_integer(value: u8) -> Option<Self> {
        Self::variants()
            .get((value as usize).wrapping_sub(1))
            .copied()
    }
}

impl ::re_byte_size::SizeBytes for AudioAnnotationKind {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        0
    }

    #[inline]
    fn is_pod() -> bool {
        true
    }
}
