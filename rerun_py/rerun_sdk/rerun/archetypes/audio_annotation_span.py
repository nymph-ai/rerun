# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_annotation_span.fbs".

# You can extend this class by creating a "AudioAnnotationSpanExt" class in "audio_annotation_span_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

import numpy as np
import pyarrow as pa
from attrs import define, field

from .. import components, datatypes
from .._baseclasses import (
    Archetype,
    ComponentColumnList,
    ComponentDescriptor,
)
from ..blueprint import VisualizableArchetype, Visualizer
from ..error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ..blueprint.datatypes import VisualizerComponentMappingLike

__all__ = ["AudioAnnotationSpan"]


@define(str=False, repr=False, init=False)
class AudioAnnotationSpan(Archetype, VisualizableArchetype):
    """
    **Archetype**: Audio-aligned annotation spans.

    Use for ASR words, utterances, speaker turns, VAD regions, TTS emissions,
    interruption windows, and other span-like evidence.

    Each row stores one or more spans using parallel arrays. Spans are logged as
    queryable data, not private UI state, so downstream tooling can overlap-query
    and export them alongside audio summaries and source metadata.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    NAME: ClassVar[str] = "rerun.archetypes.AudioAnnotationSpan"

    def __init__(
        self: Any,
        start_time: datatypes.AudioTimestampArrayLike,
        end_time: datatypes.AudioTimestampArrayLike,
        *,
        kind: components.AudioAnnotationKindArrayLike | None = None,
        labels: datatypes.Utf8ArrayLike | None = None,
        confidence: datatypes.Float64ArrayLike | None = None,
        speaker: datatypes.Utf8ArrayLike | None = None,
        source_id: datatypes.Utf8Like | None = None,
        audio_reference: datatypes.EntityPathLike | None = None,
        related: datatypes.EntityPathArrayLike | None = None,
        metadata: components.KeyValuePairsLike | None = None,
        colors: datatypes.Rgba32ArrayLike | None = None,
    ) -> None:
        """
        Create a new instance of the AudioAnnotationSpan archetype.

        Parameters
        ----------
        start_time:
            Media-time start of each span, in nanoseconds.
        end_time:
            Media-time end of each span, in nanoseconds.
        kind:
            Semantic class of each span.
        labels:
            Human-readable text for each span.
        confidence:
            Confidence score per span. Convention: 0 is lowest confidence, 1 is highest.
        speaker:
            Speaker identity or diarization label per span.
        source_id:
            Stable identity of the audio source these spans refer to.
        audio_reference:
            Entity carrying the source [`archetypes.AudioStream`][rerun.archetypes.AudioStream] or [`archetypes.AssetAudio`][rerun.archetypes.AssetAudio].
        related:
            Optional related or parent entities, e.g. words belonging to an utterance.
        metadata:
            Additional span metadata shared by this row.
        colors:
            Optional display color per span.

        """

        # You can define your own __init__ function as a member of AudioAnnotationSpanExt in audio_annotation_span_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(
                start_time=start_time,
                end_time=end_time,
                kind=kind,
                labels=labels,
                confidence=confidence,
                speaker=speaker,
                source_id=source_id,
                audio_reference=audio_reference,
                related=related,
                metadata=metadata,
                colors=colors,
            )
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            start_time=None,
            end_time=None,
            kind=None,
            labels=None,
            confidence=None,
            speaker=None,
            source_id=None,
            audio_reference=None,
            related=None,
            metadata=None,
            colors=None,
        )

    @classmethod
    def _clear(cls) -> AudioAnnotationSpan:
        """Produce an empty AudioAnnotationSpan, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        start_time: datatypes.AudioTimestampArrayLike | None = None,
        end_time: datatypes.AudioTimestampArrayLike | None = None,
        kind: components.AudioAnnotationKindArrayLike | None = None,
        labels: datatypes.Utf8ArrayLike | None = None,
        confidence: datatypes.Float64ArrayLike | None = None,
        speaker: datatypes.Utf8ArrayLike | None = None,
        source_id: datatypes.Utf8Like | None = None,
        audio_reference: datatypes.EntityPathLike | None = None,
        related: datatypes.EntityPathArrayLike | None = None,
        metadata: components.KeyValuePairsLike | None = None,
        colors: datatypes.Rgba32ArrayLike | None = None,
    ) -> AudioAnnotationSpan:
        """
        Update only some specific fields of a `AudioAnnotationSpan`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        start_time:
            Media-time start of each span, in nanoseconds.
        end_time:
            Media-time end of each span, in nanoseconds.
        kind:
            Semantic class of each span.
        labels:
            Human-readable text for each span.
        confidence:
            Confidence score per span. Convention: 0 is lowest confidence, 1 is highest.
        speaker:
            Speaker identity or diarization label per span.
        source_id:
            Stable identity of the audio source these spans refer to.
        audio_reference:
            Entity carrying the source [`archetypes.AudioStream`][rerun.archetypes.AudioStream] or [`archetypes.AssetAudio`][rerun.archetypes.AssetAudio].
        related:
            Optional related or parent entities, e.g. words belonging to an utterance.
        metadata:
            Additional span metadata shared by this row.
        colors:
            Optional display color per span.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "start_time": start_time,
                "end_time": end_time,
                "kind": kind,
                "labels": labels,
                "confidence": confidence,
                "speaker": speaker,
                "source_id": source_id,
                "audio_reference": audio_reference,
                "related": related,
                "metadata": metadata,
                "colors": colors,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> AudioAnnotationSpan:
        """Clear all the fields of a `AudioAnnotationSpan`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_start_time() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:start_time",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.AudioTimestampBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_end_time() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:end_time",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.AudioTimestampBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_kind() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:kind",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.AudioAnnotationKindBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_labels() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:labels",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.TextBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_confidence() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:confidence",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.ScalarBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_speaker() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:speaker",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.TextBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_source_id() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:source_id",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.AudioSourceIdBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_audio_reference() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:audio_reference",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.EntityPathBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_related() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:related",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.EntityPathBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_metadata() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:metadata",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.KeyValuePairsBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_colors() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioAnnotationSpan:colors",
            archetype=AudioAnnotationSpan.NAME,
            component_type=components.ColorBatch._COMPONENT_TYPE,
        )

    @classmethod
    def columns(
        cls,
        *,
        start_time: datatypes.AudioTimestampArrayLike | None = None,
        end_time: datatypes.AudioTimestampArrayLike | None = None,
        kind: components.AudioAnnotationKindArrayLike | None = None,
        labels: datatypes.Utf8ArrayLike | None = None,
        confidence: datatypes.Float64ArrayLike | None = None,
        speaker: datatypes.Utf8ArrayLike | None = None,
        source_id: datatypes.Utf8ArrayLike | None = None,
        audio_reference: datatypes.EntityPathArrayLike | None = None,
        related: datatypes.EntityPathArrayLike | None = None,
        metadata: components.KeyValuePairsArrayLike | None = None,
        colors: datatypes.Rgba32ArrayLike | None = None,
    ) -> ComponentColumnList:
        """
        Construct a new column-oriented component bundle.

        This makes it possible to use `rr.send_columns` to send columnar data directly into Rerun.

        The returned columns will be partitioned into unit-length sub-batches by default.
        Use `ComponentColumnList.partition` to repartition the data as needed.

        Parameters
        ----------
        start_time:
            Media-time start of each span, in nanoseconds.
        end_time:
            Media-time end of each span, in nanoseconds.
        kind:
            Semantic class of each span.
        labels:
            Human-readable text for each span.
        confidence:
            Confidence score per span. Convention: 0 is lowest confidence, 1 is highest.
        speaker:
            Speaker identity or diarization label per span.
        source_id:
            Stable identity of the audio source these spans refer to.
        audio_reference:
            Entity carrying the source [`archetypes.AudioStream`][rerun.archetypes.AudioStream] or [`archetypes.AssetAudio`][rerun.archetypes.AssetAudio].
        related:
            Optional related or parent entities, e.g. words belonging to an utterance.
        metadata:
            Additional span metadata shared by this row.
        colors:
            Optional display color per span.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            inst.__attrs_init__(
                start_time=start_time,
                end_time=end_time,
                kind=kind,
                labels=labels,
                confidence=confidence,
                speaker=speaker,
                source_id=source_id,
                audio_reference=audio_reference,
                related=related,
                metadata=metadata,
                colors=colors,
            )

        batches = inst.as_component_batches()
        if len(batches) == 0:
            return ComponentColumnList([])

        kwargs = {
            "AudioAnnotationSpan:start_time": start_time,
            "AudioAnnotationSpan:end_time": end_time,
            "AudioAnnotationSpan:kind": kind,
            "AudioAnnotationSpan:labels": labels,
            "AudioAnnotationSpan:confidence": confidence,
            "AudioAnnotationSpan:speaker": speaker,
            "AudioAnnotationSpan:source_id": source_id,
            "AudioAnnotationSpan:audio_reference": audio_reference,
            "AudioAnnotationSpan:related": related,
            "AudioAnnotationSpan:metadata": metadata,
            "AudioAnnotationSpan:colors": colors,
        }
        columns = []

        for batch in batches:
            arrow_array = batch.as_arrow_array()

            # For primitive arrays and fixed size list arrays, we infer partition size from the input shape.
            if pa.types.is_primitive(arrow_array.type) or pa.types.is_fixed_size_list(arrow_array.type):
                param = kwargs[batch.component_descriptor().component]  # type: ignore[index]
                shape = np.shape(param)  # type: ignore[arg-type]
                num_rows = shape[0] if len(shape) >= 1 else 1  # type: ignore[redundant-expr,misc]

                if pa.types.is_fixed_size_list(arrow_array.type):
                    elem_flat_len = int(np.prod(shape[1:])) if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                    if arrow_array.type.list_size == elem_flat_len:
                        # The product of the last dimensions of the shape are equal to the size of the fixed size list array,
                        # so we have `num_rows` single element batches (each element is a fixed sized list).
                        batch_length = 1
                    else:
                        batch_length = shape[1] if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                else:
                    # For primitive types, derive batch_length from the actual arrow array length
                    # since the input shape can be misleading (e.g. colors [R,G,B] -> single uint32).
                    batch_length = len(arrow_array) // num_rows if num_rows > 0 else 1

                sizes = batch_length * np.ones(num_rows)
            else:
                # For non-primitive types, default to partitioning each element separately.
                sizes = np.ones(len(arrow_array))

            columns.append(batch.partition(sizes))

        return ComponentColumnList(columns)

    start_time: components.AudioTimestampBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.AudioTimestampBatch._converter,  # type: ignore[misc]
    )
    # Media-time start of each span, in nanoseconds.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    end_time: components.AudioTimestampBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.AudioTimestampBatch._converter,  # type: ignore[misc]
    )
    # Media-time end of each span, in nanoseconds.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    kind: components.AudioAnnotationKindBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.AudioAnnotationKindBatch._converter,  # type: ignore[misc]
    )
    # Semantic class of each span.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    labels: components.TextBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TextBatch._converter,  # type: ignore[misc]
    )
    # Human-readable text for each span.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    confidence: components.ScalarBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ScalarBatch._converter,  # type: ignore[misc]
    )
    # Confidence score per span. Convention: 0 is lowest confidence, 1 is highest.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    speaker: components.TextBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TextBatch._converter,  # type: ignore[misc]
    )
    # Speaker identity or diarization label per span.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    source_id: components.AudioSourceIdBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.AudioSourceIdBatch._converter,  # type: ignore[misc]
    )
    # Stable identity of the audio source these spans refer to.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    audio_reference: components.EntityPathBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.EntityPathBatch._converter,  # type: ignore[misc]
    )
    # Entity carrying the source [`archetypes.AudioStream`][rerun.archetypes.AudioStream] or [`archetypes.AssetAudio`][rerun.archetypes.AssetAudio].
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    related: components.EntityPathBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.EntityPathBatch._converter,  # type: ignore[misc]
    )
    # Optional related or parent entities, e.g. words belonging to an utterance.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    metadata: components.KeyValuePairsBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.KeyValuePairsBatch._converter,  # type: ignore[misc]
    )
    # Additional span metadata shared by this row.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    colors: components.ColorBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ColorBatch._converter,  # type: ignore[misc]
    )
    # Optional display color per span.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]

    def visualizer(self, *, mappings: list[VisualizerComponentMappingLike] | None = None) -> Visualizer:
        """
        Creates a visualizer for this archetype, using all currently set values as overrides.

        Parameters
        ----------
        mappings:
            Optional component mappings to control how the visualizer sources its data.

            ⚠️ **Experimental**: Component mappings are an experimental feature and may change.
            See https://github.com/rerun-io/rerun/issues/10631 for more information.

        """
        return Visualizer("AudioAnnotationSpan", overrides=self.as_component_batches(), mappings=mappings)
